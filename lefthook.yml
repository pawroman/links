pre-commit:
    parallel: true
    commands:
        black:
            run: black --check ci/
        mypy:
            run: mypy ci/
        quick-links-test:
            run: pytest -m 'not external' -q --disable-warnings
