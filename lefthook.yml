pre-commit:
  piped: true
  commands:
    format:
      run: just format
      priority: 1
      stage_fixed: true

    format_isort:
      run: just format_isort
      priority: 2
      stage_fixed: true

    check:
      run: just check

    test:
      run: just test -q
